case: L3

dataset:
  type: ST3D
  data_file: data/datasets/data_SUICA/ST3D/${case}.h5ad
  val_proportion: 0.2
  keep_ratio: True

pipeline:
  target: raw_representations

  inr:
    model: SIREN
    num_hidden_layers: 12
    num_hidden_features: 2048 
    decoder: # will be automatically updated

  optimization:
    seed: 8848
    epochs: 2000
    lr: 1e-5
    val_freq: 200
    logs: logs/SIREN/${case}
    batch_size: 8192

  predict_mode: val
  reconstructed_data: reconstructed-val.h5ad